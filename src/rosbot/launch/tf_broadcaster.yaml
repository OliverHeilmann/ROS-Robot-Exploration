# to launch, use:
#   ros2 launch package launch_file [arguments]

# This is a launch file that starts the tf_broadcaster node.
launch:
  - node:
      pkg: "rosbot"
      exec: "tf_broadcaster"
      name: "tf_broadcaster"